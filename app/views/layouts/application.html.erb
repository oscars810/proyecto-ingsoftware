<!DOCTYPE html>
<html>
<head>
    <title>MatchMeat</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script defer src="https://use.fontawesome.com/releases/v5.3.1/js/all.js"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.8.2/css/bulma.min.css">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
<<<<<<< HEAD
  </head>

  <body>
  <h1 style="text-align:center"> Match & Meat </h1>
  <% if user_signed_in? %>
    <p style="text-align:right"> Hola, <%= current_user.nombre %>   <%= link_to '(Cerrar Sesion)', destroy_user_session_path, method: :delete %> </p>
  <% end %>
    <hr>
    <table style="table-layout: fixed; width: 100%">
        <th>
            <td> <%= link_to 'Inicio', root_path%> </td>
            <td> <%= link_to 'Locales', local_index_path %> </td>
            <% if user_signed_in? %>
              <td> <%= link_to 'Match', match_path(current_user.id)%> </td>
            <% end %>
            <% if user_signed_in? %>
              <td> <%= link_to 'Mi Perfil', perfil_path(current_user.id)%> </td>
            <% end %>
            <% unless user_signed_in? %>
                <td> <%= link_to 'Iniciar Sesion', new_user_session_path %></td>
            <% end %>
            <% unless user_signed_in? %>
                <td> <%= link_to 'Registrarse', new_user_registration_path %></td>
            <%end%>
            <% if user_signed_in? and current_user.admin%>
                <td><%= link_to 'Administrar', admin_path %></td>
            <%end%>
        </th>
    </table>
    <hr>
    <p id="notice"><%= notice %></p>
    <%= yield %>
  </body>
=======
</head>
<body>
  <%= render 'layouts/navbar' %>
  <section class="hero is-fullheight has-bg-img has-text.white">
      <p id="notice"><%= notice %></p>
      <%= yield %>
  </section>
</body>
>>>>>>> develop
</html>
