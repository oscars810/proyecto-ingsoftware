<div class="container">
    <h1 class="title is-5 has-text-white">Locales Pendientes</h1>

    <hr>
    <table class="table">
    <thead>
        <tr>
        <th>Nombre</th>
        <th>Descripción</th>
        <th colspan="3"> Acciones </th>
        </tr>
    </thead>

    <tbody>
        <% @locales_pendientes.each do |local_| %>
        <tr>
            <td><%= local_.nombre %></td>
            <td><%= local_.descripcion %></td>
            <td><%= link_to 'Aceptar Local', "aceptar_locales/#{local_.id}", method: :patch%></td>
        </tr>
        <% end %>
    </tbody>
    </table>

    <br>
    <%= link_to 'Volver al Panel', admin_path, style: "color:white"%>
</div>
