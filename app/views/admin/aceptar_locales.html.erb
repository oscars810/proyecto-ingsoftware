<section class="section">
<div class="box">
<h1 class="title is-5 has-text-centered">Aceptar locales</h1>

<table class="table is-fullwidth is-striped">
<thead>
    <tr>
    <th>Nombre</th>
    <th>Descripción</th>
    <th>Acciones</th>
    </tr>
</thead>

<tbody>
    <% @locales_pendientes.each do |local| %>
    <tr>
        <td><%= local.nombre %></td>
        <td><%= local.descripcion %></td>
        <td><%= link_to 'Aceptar local', admin_aceptar_local_path(local.id), method: :patch, class: "button is-primary"%></td>
    </tr>
    <% end %>
</tbody>
</table>

<%= link_to 'Volver al Panel', admin_path, class: "button is-link"%>
</div>
</section>