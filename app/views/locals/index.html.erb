<head>
  <title>MatchMeat</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script defer src="https://use.fontawesome.com/releases/v5.3.1/js/all.js">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.8.2/css/bulma.min.css">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %></script>
</head>

<h1>Aqui podras ver todos los locales que tenemos para tus citas:</h1>
<p>No olvides preguntar por descuentos al hacer citas a traves de nuestra aplicación.</p>

<h1>Locales</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Comuna</th>
      <th>Descripcion</th>
      <th colspan="3"> Acciones </th>
    </tr>
  </thead>

  <tbody>
    <% @locales.each do |local| %>
      <tr>
        <% if local.aceptado %>
            <td><%= local.nombre %></td>
            <td><%= local.commune.nombre %></td>
            <td><%= local.descripcion %></td>
            <td><%= link_to 'Ver detalles', local_path(local.id) %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>